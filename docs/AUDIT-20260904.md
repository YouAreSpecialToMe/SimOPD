# 全项目审查报告(2026-09-04)

> 对象:`ch-dev` @ 6d89ba2 起的整个仓库(284 个 tracked 文件:scripts 95、docs 71、deploy 51、src 22、
> slurm 18、paper 10、configs 6)。方法:逐层机械核对 —— 编译/语法、登记表解析、钩子链、环境旋钮、
> 写死路径、文档交叉引用、归档数据可读性、分析管线可重生成、本地电池、名册 37 条逐臂科学配置、
> 舰队发射路径。**能在没有 GPU 的机器上核的都核了;需要卡的三件(vLLM 起得来、eval_suite 在新栈跑通、
> 单卡显存够 32k)仍然没验。**
>
> 严重度:**S1** = 照文档开跑会跑错/跑不起来且不报错;**S2** = 会报错但文档没写、或结论表述错;
> **S3** = 卫生/可维护性。每条标 ✅(本轮已修)/ ⏳(未修,给出做法)。

## 0 一句话

代码本身很干净(108 个 py、78 个 sh 全部编译/语法通过;103 条臂登记唯一、YAML 可解析、99 条可
`arm.py env`;八个 sitecustomize 钩子对应模块全在;分析管线能从归档一键重生成两张页面)。
**问题集中在"只存在于旧盘的东西"和"登记 ↔ 发射门 ↔ 裁决名单三处不同步"**:六条名册臂的资产指向
已失联的盘、四条新臂会被发射门拦下、十三条臂的裁决永远不打印、二十来个产生证据的分析脚本从未进 git。

## 1 Findings(按严重度)

### S1 —— 静默跑错 / 静默不起

| # | 发现 | 证据 | 状态 |
|---|---|---|---|
| 1.1 | **六条名册臂的 env 写死旧盘绝对路径**:a1/a3/a4 的 `SIMOPD_GKD_CACHE`、a5/h6/h9 的 `*_KEYS` 全是 `/mgfs/shared/Group_GY/changhao/simopd_data/gkd_offpolicy.parquet(.dry)`;`arm.py env` 不展开变量,舰队起 lane 前无资产断言 → 新集群上照样起 lane,vLLM 起完后才在 `gkd_mix.install()` 炸(a5 同) | `arms.yaml` 14 处 `/mgfs`;`arm.py` 无 `expandvars`;`corr_wave_fleet.sh` 无 Phase P | ✅ 路径改 `$SIMOPD_STORE/...`;`arm.py` 展开、展不开拒绝;舰队 `_launch_lane` 前断言资产;五个调用方导出 `SIMOPD_STORE` |
| 1.2 | **四条新登记臂不在 `arm_lint.EXPECT_PG`**(vanilla_te / c2_qb_fixed8_corr / c2_qb_perseq_corr / h5_gen100_n0)→ lint 报 "not in the lint's branch table" → 该项**不在**舰队发射门的豁免正则(`campaign.tsv row|verdict.py ARMS`)里 → 含这四条的 slot **lanes NOT launched**,每 2 分钟打一行 | `arm_lint.py` EXPECT_PG 99 条;`corr_wave_fleet.sh` scoped gate | ✅ 补四条(分支照对照臂) |
| 1.3 | **派生数据的生成从未写进交接文档**:`gkd_offpolicy.parquet`(GPU、贵、一次性)、`.dry` 键文件(CPU)、`train_sub50.parquet`(CPU)是 7 条名册臂的前置,旧盘已失联 = 必须重生成 | `gen_offpolicy.py` / `gen_task_subset.py` 存在,AGENT.md 零提及 | ✅ AGENT.md §6 1d |
| 1.4 | **`setup.sh` 拉 verl 上游 HEAD 不钉 commit**(`--depth 1`),而八个钩子挂在 verl/vLLM 内部符号上 | `setup.sh:296`;本地 verl = `aebd1f8`(2026-07-31,无未提交补丁) | ✅ 钉 `aebd1f8`,`VERL_COMMIT=` 可换;§5 记全栈版本 |

### S2 —— 会响但没写、或表述错

| # | 发现 | 证据 | 状态 |
|---|---|---|---|
| 2.1 | **`c4_carrier` / `c4_rep` 被 AGENT.md §1 / §2.1 / §2.2 记成"修正内核的独立复现 / 修而不折叠"**。`c4_rep` 登记为 TOPK 32、无载体、无 TE —— 根本没修 eos;它 .353 不塌是因为旧载体上的 top-k 族本来就不塌(`c4_pi_tail_budget` 三种子 @250 = .353/.354/.357,截断 .08)。这正是"药效 = 抗塌缩"的另一面,不是治愈的复现 | `arms.yaml` c4_rep 条目;`post_eval_bystep.csv` | ✅ §1/§2.1/§2.2 改定性,并写明唯一的真复现是 vanilla → vanilla_corr |
| 2.2 | **13 条名册臂不在 `verdict.py ARMS`**(四条新臂 + 九条 `_n0`)→ 裁决行"就是不会打印";lint 会报但该项被发射门豁免,所以没人看见 | `verdict.py` ARMS | ✅ 补 13 条 + `BASE_OVERRIDES` 3 条(vanilla_te→vanilla_corr;c2 梯子→c2_quantile_budget_corr) |
| 2.3 | **`traj_dump_battery.py` 红了两处**:① 09-02 重写把每行的 `stop_set` 列挪进 `meta.json`,电池(和按契约分组的离线分析)读的是行级列;② 电池假设包装器总调 verl 原函数,而新默认 `SIMOPD_TRAJ_TEXT=0` 不调 | 电池输出 | ✅ ① 列加回 `_light_row` / `light.jsonl`;② 该用例显式 `TEXT=1`。电池 26/26 绿 |
| 2.4 | **~20 个产生 `docs/data/` 证据的分析脚本从未进 git**:`eos_stop_audit.py`(头号机制 `eos_stop_audit.txt` 的生成器)、`eval_offline_textdump.py`、`analyze_diag.py` / `analyze_order.py` / `analyze_pg.py`、`emit_dynamics.py` / `extract_metrics.py`、`clock_b2.py` / `clock_n0.py` / `probe_b2.py` / `style_anatomy.py` / `v2probe_paired.py`、`a2_coldstart_probe.py`、`exp_publish.py`、`compute_ledger.py` …… 与 `eval_worker_exp.sh` 同一死法 | 文档交叉引用核对:264 处引用,约 80 处指空,其中这类占大半 | ⏳ **不可恢复**。规矩写进 §7:凡写 `docs/data/` 的脚本一律进 `scripts/analysis/`。头号机制的证据文件仍在(`docs/data/eos_stop_audit.txt`),但生成过程只剩 MECHANISMS §M-I 的文字描述 |
| 2.5 | **`README.md` 停在 08-11**:讲的是"远程协作者认领 remote 臂"的模式,没有指向 AGENT.md,不提 ch-dev 是唯一活分支(`main` 只有 init 提交,ch-dev 领先 720) | `git log` | ⏳ 建议 README 顶部加三行:活分支、入口 AGENT.md、审查报告 |
| 2.6 | **49 个 `SIMOPD_*` 旋钮代码里读、任何文档没提**,其中约一半是科学旋钮(`C4_TAU_H/Q`、`FIRE_ALPHA/BETA`、`SELECTKD_K/BETA`、`H_BUDGET`、`REP_GATE_*`、`PI_TAIL_EPS`、`EOS_AUX_COEF`、`B3_SOFT_COEF`、`CLIP_M`、`LOSS_AGG_MODE` …) | `git grep` 代码 96 vs 文档 47 | ⏳ 建议一个生成器从 `src/` 抓 `os.environ.get("SIMOPD_...")` 连同默认值出一张表进 `docs/KNOBS.md`;登记表已覆盖其中大部分的**取值**,缺的是**语义** |
| 2.7 | **`f2_hard_clip_corr` / `g6_seqmean_corr` 的 eos 修正依赖启动器默认 `k1_rec`**(它们不写 `DISTILLATION_LOSS_MODE`,靠默认落进 `TERM_EVENT_FAMILY`) —— 现在是对的,但谁改了默认值这两条就静默失修 | `arms.yaml` note 已说明;`run_opd_baseline.sh:75` | ⏳ 建议登记表显式写 `DISTILLATION_LOSS_MODE: k1_rec`(纯冗余,零风险) |

### S3 —— 卫生 / 可维护性

| # | 发现 | 状态 |
|---|---|---|
| 3.1 | 56 MB 的 `flash_attn-2.8.3.post1-cp312...whl` 进了 git(`deploy/dsw/`) | ⏳ 建议移到 release asset / 对象存储,仓库留 URL + sha256 |
| 3.2 | 死层未归档:`slurm/`(18 个,Cornell 时代,08-03~08-10)、`deploy/fix_m*.sh` / `up.sh` / `campaign*.sh`(DSW m1-m3 时代)、`deploy/dsw/launch_m*.sh`;`deploy/dlc/a_axis_fleet.sh` / `h_axis_fleet.sh` 已被 `corr_wave_fleet.sh` 覆盖 | ⏳ 建议移到 `deploy/_retired/` 并在 README 标"只作考古" |
| 3.3 | 29 个 scripts 无任何引用(`whatsup.sh`、`deepcheck.sh`、`run_status.py`、`family_diag.py`、`compare_arms.py`、`crossarm.py`、`stamp_killed.py`、`wandb_backfill_groups.py`、`post_install.sh`、`make_post_eval_page.py` …);其余是电池(靠人手跑) | ⏳ 电池应有一个 `scripts/run_batteries.sh` 汇总入口(本轮 8/10 本地能跑);其余逐个判死 |
| 3.4 | `configs/campaign.tsv`(236 行,08-20 停更)仍被 AGENT.md 抬头称"派工真源",实际本轮派工走 lane 图;只剩 `arm_lint` 读它(且该项被发射门豁免) | ✅ 抬头改口 |
| 3.5 | 硬编码路径:53 个文件含 `/mgfs/shared`、18 个 `/scratch/zz865`、23 个 `/home/zz865`。舰队/评测/彩排/登记表这几处关键的已给覆盖口;其余多在 `scripts/analysis/*launch.sh`、`slurm/`、`audit_domains_cpu.sh` 等旧层 | ⏳ 随 3.2 归档 |
| 3.6 | 没有 `tests/`,电池散在 `scripts/`;本地可跑 8 个(stop_set / n2_eos / h9 / h_horizon / gkd_schedule / a5 / traj_dump / eval_worker),`union` / `c4_state` 要 verl,`test_domain_reward` 要 `absl` | ⏳ 同 3.3 |
| 3.7 | `docs/late-training-collapse.md` 引用 18 个不存在的文件(多为逐题 json 与临时脚本);`PROTOCOL-unified.md` 引 `logs/early_stops.tsv` 等 6 个 | ⏳ 历史文档,标"数据已随旧盘失联"即可 |

## 2 通过的项(不用再查)

- 编译/语法:python 108/108,shell 78/78。
- 登记表:103 条 run_id 唯一;YAML 解析;`arm.py env` 99 通过、4 拒绝均为 `shelved`/`needs`(应拒)。
- 钩子链:`REQUIRED_MODULES` 18 个模块全在;`_TARGETS` 8 个钩子;src 内 `simopd.*` import 全部可解析。
- 名册 37 条科学配置逐臂核过(表在 `/tmp/roster_env.tsv` 的生成命令见本报告 §4):所有 `_corr` 臂
  `GATHER_EOS=1 + KEEP_SAMPLED=1 + E_S={151643} / E_T={151643,151645} / STOP=off`;k1 族与 top-k 族
  `TERM_EVENT=1`;例外恰好是 ARM-REVIEW §6.5 声明的三条(c5 两条 union、c4_hq/c4_state);
  九条 `_n0` 走 v2 契约(`E_S={151643,151645}`,`STOP=151645`);A 轴四条、H 预算线六条**彼此确实不同**
  (λ / schedule / keys / 子集 各异,不是"四条 vanilla")。
- 归档数据:`post_eval_cells` 4700 行、`post_eval_bystep` 895 行 57 臂、`inloop_wave_dynamics` 51 run、
  三份 `*_allkeys.csv.gz` 可读;`collapse_status.py` / `make_cure_page.py` / `make_dynamics_page.py` /
  `make_campaign_tables.py` 全部 rc=0,重生成只改一行时间戳。
- 凭据卫生:tracked 文件无 key/token 形态字符串;`simopd_env.sh` / venv / verl 在 `.gitignore`。
- 本地 verl/ 无未提交改动(所以钉 commit 即可重建)。
- 上一轮修的舰队三处(步数第三段、内置表守卫、`SIMOPD_STORE`)与 eval worker 35 条断言,本轮重跑仍绿。

## 3 本轮改了什么(全部已提交)

| 文件 | 改动 |
|---|---|
| `configs/arms.yaml` | 14 处 `/mgfs/.../simopd_data` → `$SIMOPD_STORE` |
| `scripts/arm.py` | env 值 `expandvars`;残留 `$VAR` 则拒绝并指到 §6 第 0 步 |
| `deploy/dlc/corr_wave_fleet.sh` | `export SIMOPD_STORE=$D`;`_launch_lane` 前资产断言(缺则不起、不三连重试) |
| `deploy/dsw/rehearse_n2.sh`、`deploy/dlc/a_axis_fleet.sh`、`h_axis_fleet.sh`、`lane_once.sh` | `D` 认 `SIMOPD_STORE` 并导出 |
| `deploy/dsw/setup.sh` | verl 钉 `aebd1f8`(`VERL_COMMIT=` 可换),不一致时警告 |
| `scripts/arm_lint.py` | `EXPECT_PG` +4 |
| `scripts/verdict.py` | `ARMS` +13,`BASE_OVERRIDES` +3 |
| `src/simopd/traj_dump.py` | 行级 `stop_set` 列加回 |
| `scripts/traj_dump_battery.py` | "原函数照常执行"用例显式 `SIMOPD_TRAJ_TEXT=1` |
| `scripts/make_lane_map.py` | 未设 `SIMOPD_STORE` 时用占位符只验臂名(并明说) |
| `AGENT.md` | 抬头指针;§1 / §2.1 / §2.2 的 c4 定性;§5 栈版本;§6 1d 派生数据;§7 四个新坑 |

## 4 怎么复核这份报告(命令)

```bash
# 编译/语法
for f in $(git ls-files '*.py'); do python3 -m py_compile "$f" || echo FAIL $f; done
for f in $(git ls-files '*.sh'); do bash -n "$f" || echo FAIL $f; done
# 登记表 / 名册
python3 scripts/arm.py check | head -3
SIMOPD_STORE=/tmp/x python3 scripts/make_lane_map.py --print     # 37 条臂名逐条过 arm.py env
# 电池(无 GPU)
bash deploy/dlc/test_eval_worker.sh
PYTHONPATH=src python3 scripts/traj_dump_battery.py               # 需要 torch
for b in stop_set n2_eos h9 h_horizon gkd_schedule a5; do PYTHONPATH=src python3 scripts/${b}_battery.py; done
# 分析管线可重生成
python3 scripts/analysis/collapse_status.py && python3 scripts/analysis/make_cure_page.py \
  && python3 scripts/make_dynamics_page.py && python3 scripts/make_campaign_tables.py && git diff --stat
# 文档交叉引用 / 写死路径
git grep -c '/mgfs/shared' -- . ':!docs/*' ':!*.md' | sort -t: -k2 -rn | head
```

## 5 还没被证明、只能在有卡的地方验的

1. 真 vLLM 0.26 + verl `aebd1f8` 在新集群的 CUDA/driver 上起得来(钩子 `first gathered rows via …` 收据行)。
2. `eval_suite.py run` 在新栈跑通、单卡显存够 32768 token(k=8)。
3. 彩排 Phase R 35 条全过;`gen_offpolicy.py` 在新集群生成的缓存与旧盘那份**不是同一份**
   (教师采样有随机性,seed 固定但硬件/vLLM 版本可能改变采样)—— A 轴四条的"离策来源"因此是新集群的,
   论文里声明即可,不影响臂内对比。
