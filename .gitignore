# third-party clones (see README for setup)
verl/
OPD/
EasyOPD/

# system / editor
.DS_Store
__pycache__/
*.pyc
.venv/
venv/
/simopd/   # the venv at <repo>/simopd. Anchored: a bare `simopd/` also
           # matches src/simopd/, which silently kept zmq_lane.py out of git for a
           # day while sitecustomize imported it on four machines that never had it.

# experiment outputs
logs/
outputs/
checkpoints/
wandb/
*.log

.campaign/
